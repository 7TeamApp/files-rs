export const jsCopyGlobMulty = (pattern: string[], outDir: string) => {};
export const jsCopyGlobSingle = (pattern: string, outDir: string) => {};
export const jsCopyMulty = (path: string[], outDir: string) => {};
export const jsCopySingle = (path: string, outDir: string) => {};
